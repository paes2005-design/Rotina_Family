name: Validate dashboard pro
on:
  push:
    paths:
      - .github/workflows/check-dashboard-pro.yml
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node --check dashboard-ranking-pro.js